/* let nome = prompt("Qual é o seu nome?");
alert(nome);
 */

let lista = [992, -12, 3, 13, 15, 30, 33, 67, 22, 0];
let tamanho = lista.length;

console.log(tamanho);

for (let contador = 0; contador < tamanho; contador++) {
  console.log(lista[contador]);
}
